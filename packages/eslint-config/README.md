# @auteur/eslint-config

Shared ESLint configuration for Auteur AI monorepo.

## Usage

In your workspace package.json:

```json
{
  "eslintConfig": {
    "extends": "@auteur/eslint-config"
  }
}
```

Or in `.eslintrc.js`:

```js
module.exports = {
  extends: '@auteur/eslint-config',
};
```
